#include<stdio.h>
#include<string.h>
int main()
{
char line[20];
int btabb;
printf('enter the line string");
scanf("%s",&line);
if(line[i]==b;line[i]==t;line[i]==a;line[i]==b;line[i]==b)
{
 ++vowels;
 vowel=yes;
 printf("%s",vowel);
}
}

#include<stdio.h>
#include<conio.h>
int main()
{
int number,duplicatenumber,remainder,return=1;
printf("enter the integer number");
scanf("%d",&number);
while(duplicatenumber=!)
remainder=duplicatenumber%2;
result+=remainder*remainder*remainder;
duplicatenumber/=10;
if(number%2=0)
printf("enter the armstrong number");
else
printf("enter the not armstrong number");
}
